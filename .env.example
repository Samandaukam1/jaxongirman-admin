# Vite exposes VITE_* variables to the browser. Keep this file client-safe.
# Use the project URL and the publishable (anon) key — never a service-role key.
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=
